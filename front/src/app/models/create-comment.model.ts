/**
 * Corps de création d'un commentaire (miroir du `CreateCommentRequest` back).
 * Seul le contenu est fourni : auteur et date sont posés serveur.
 */
export interface CreateCommentRequest {
  content: string;
}